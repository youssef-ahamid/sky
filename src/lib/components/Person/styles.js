export const PersonWrapper = props => {
  return [
    {
      classes: '',
      styles: '',
      on: true, // default
    },
  ]
}
export const TextWrapper = props => {
  return [
    {
      classes: '',
      styles: '',
      on: true, // default
    },
  ]
}
