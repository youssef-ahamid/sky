const cursorStyles =
  'cursor-none w-8 transition duration-150 ease-out z-50 fixed -translate-y-1/2 hidden hover:block peer-hover:block'
const cursorWrapperStyles = 'cursor-none peer max-w-min inline-block'

export const classes = {
  cursor: cursorStyles,
  cursorWrapper: cursorWrapperStyles,
}
